# Test Run — 2026-09-17

## Summary

Fixed three test issues and ran the full test suite against the isolated PostgreSQL test stack in Docker. All 78 tests passed.

## Test fixes

- `tests/test_data_integrity.py`: use the seeded `owner_id` consistently for the order, first checkout, and duplicate-checkout rejection. This avoids relying on a hard-coded user ID whose role may not allow checkout.
- `tests/test_cashier_workflow.py`: assert that the test order enters and leaves the cashier queue without assuming the shared test database has no other open orders.
- `tests/test_cashier_workflow.py`: scope the rollback audit-log assertion to the order created by that test, rather than requiring the entire database to contain no checkout audit logs.

## Verification

Command:

```powershell
docker compose -f docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from test-runner
```

Result:

```text
Ran 78 tests in 8.470s
OK
TEST SUMMARY: Ran 78 tests
Passed: 78
Failures: 0
Errors: 0
ALL SANDBOX VERIFICATION TESTS PASSED 100%!
```

The test runner exited with code 0. Both isolated test containers stopped cleanly with exit code 0. The separate `posflet-app` and `posflet-db` containers remained healthy. The test stack targeted `pos_test_db`; production `pos_db` was not used by the test runner.

`git diff --check` passed. Git emitted existing line-ending (LF/CRLF) warnings for modified working-tree files; no whitespace errors were reported.

## Scope note

These results verify the current automated suite (78 tests); they do not establish that every possible application behavior has been tested. Nexus session persistence was unavailable because its project-ID validation rejected both `POS flet` and the alphanumeric ID `posflet`.
